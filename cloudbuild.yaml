steps:

  - id: build-image
    name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile_with_streaming', '.', '-t', 'gcr.io/$PROJECT_ID/bitcoin-etl:${TAG_NAME}']

  - id: push-image
    name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/bitcoin-etl:${TAG_NAME}']
    waitFor: ['build-image']

timeout: 600s